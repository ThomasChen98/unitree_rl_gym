# Configuration for H1_2 robot with hybrid control:
# - Lower body (12 DOF): controlled by policy
# - Upper body (15 DOF): controlled by predefined trajectories
policy_path: "{LEGGED_GYM_ROOT_DIR}/logs/h1_2/exported/policies/policy_lstm_1.pt"
xml_path: "{LEGGED_GYM_ROOT_DIR}/resources/robots/h1_2/scene.xml"

# Total simulation time
simulation_duration: 60.0
# Simulation time step
simulation_dt: 0.002
# Controller update frequency (meets the requirement of simulation_dt * controll_decimation=0.02; 50Hz)
control_decimation: 10

# PD gains for lower body (12 DOF) - same as original
lower_body_kps: [200, 200, 200, 300, 40, 40, 
                 200, 200, 200, 300, 40, 40]
lower_body_kds: [2.5, 2.5, 2.5, 4, 2, 2,  
                 2.5, 2.5, 2.5, 4, 2, 2]

# PD gains for upper body (15 DOF)
upper_body_kps: [100,  # torso
                 50, 50, 30, 30, 20, 20, 20,  # left arm
                 50, 50, 30, 30, 20, 20, 20]  # right arm
upper_body_kds: [5.0,  # torso
                 2.0, 2.0, 1.5, 1.5, 1.0, 1.0, 1.0,  # left arm
                 2.0, 2.0, 1.5, 1.5, 1.0, 1.0, 1.0]  # right arm

# Default angles for lower body (12 DOF) - same as original
lower_body_default_angles: [0, -0.16, 0.0, 0.36, -0.2, 0.0, 
                           0, -0.16, 0.0, 0.36, -0.2, 0.0]

# Default angles for upper body (15 DOF)
upper_body_default_angles: [0.0,  # torso
                           0.0, 0.0, 0.0, 1.5, 0.0, 0.0, 0.0,  # left arm
                           0.0, 0.0, 0.0, 1.5, 0.0, 0.0, 0.0]  # right arm

# Policy parameters (same as original for lower body)
ang_vel_scale: 0.25
dof_pos_scale: 1.0
dof_vel_scale: 0.05
action_scale: 0.25
cmd_scale: [2.0, 2.0, 0.25]
num_actions: 12  # Only lower body actions from policy
num_obs: 47

cmd_init: [0, 0, 0]

# Upper body trajectory parameters
trajectory_amplitude: 0.5  # Amplitude for arm circular motion (radians)
trajectory_frequency: 0.5  # Frequency of circular motion (Hz)
torso_bend_amplitude: 0.2  # Maximum torso forward bend (radians)
torso_bend_frequency: 0.3  # Frequency of torso bending (Hz)
arm_forward_offset: 0.5    # Base forward position for arms (radians)